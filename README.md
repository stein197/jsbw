# Easier bitwise operations
The package provides a tiny set of functions to make work with bit mask much easier.

## Installation
```
npm install @stein197/bw
```
Or directly from [release page](releases)

## Usage
```ts
```

# NPM scripts
- `build` builds the project
- `test` builds and tests the project
